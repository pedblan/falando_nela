# Arquivo histórico

Esta pasta preserva implementações, cadernos, testes e contratos encerrados.
Seu conteúdo não integra o pipeline científico ativo e não deve ser executado
como se representasse a metodologia corrente.

Nada é apagado ao ser arquivado. O histórico Git continua sendo a referência
para reproduzir o estado exato de cada tentativa.

Arquivos atuais:

- `pipeline_pos_coleta_v1_abortado_20260724/`: normalização v1, derivados,
  inventário, tentativa de snapshot v2 e specs do primeiro reinício.

A análise de plenário v1 anteriormente encerrada continua preservada em
`notebooks/arquivo/analise_plenario_v1_abortada_20260723/`.
